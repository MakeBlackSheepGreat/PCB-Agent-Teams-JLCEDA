# Fab Vendor 选项（JLCPCB vs PCBWay）

> 来源：`.claude/skills/fab/references/jlcpcb.md` / `.claude/skills/fab/references/pcbway.md`
> 生成于 {{ generated_at }}

## 工艺对比

| 项目 | JLCPCB | PCBWay |
|---|---|---|
| 最小线宽 / 间距 | 3.5 mil / 3.5 mil | 3 mil / 3 mil |
| 最小孔径 | 0.2 mm（带铜） | 0.15 mm（激光钻） |
| 板厚范围 | 0.4–2.0 mm | 0.2–6.0 mm |
| 铜厚 | 1oz–2oz（标） / 4oz 加价 | 1oz–6oz |
| 阻焊颜色 | 绿/红/黄/蓝/白/黑 | 绿/红/黄/蓝/白/黑/紫/哑光黑 |
| 装配方式 | SMT 元件需在 LCSC 库 | turnkey（全球采购） |
| 起订 | 5 片 | 5 片 |

## 当前板子的 turnkey 适配

- LCSC 覆盖率：{{ coverage.single_vendor_coverage.lcsc }} → {% if "lcsc" in coverage.recommended_paths %}**JLCPCB 一站式可行**{% else %}**JLCPCB turnkey 不全，建议 PCBWay 或 PCB 独打 + 自买元件**{% endif %}
- DigiKey JP 覆盖率：{{ coverage.single_vendor_coverage.digikey_jp }}
- Mouser JP 覆盖率：{{ coverage.single_vendor_coverage.mouser_jp }}

## 用户偏好驱动的推荐

Phase 2 渠道偏好：**{% if user_intent.channel == "lcsc_jlcpcb" %}LCSC 拼 JLCPCB → JLCPCB turnkey 首选{% elif user_intent.channel == "jp_domestic_fast" %}日本仓快速 → 元件走 DK JP / Mouser JP，PCB 单独 JLCPCB 打{% else %}自动选最便宜 lane{% endif %}**

LCSC 覆盖率 {{ coverage.single_vendor_coverage.lcsc }}：
{%- if user_intent.channel == "lcsc_jlcpcb" %}
{%- if "lcsc" in coverage.recommended_paths %}
- ✅ 全覆盖，JLCPCB turnkey 直接可行（按你偏好走）
{%- else %}
- ⚠ 不全：你偏好 LCSC 拼单但有缺件，下面 2 个选项二选一：
  - 改用 PCBWay turnkey（跨 distributor 采购，但贵 ~2x）
  - 改 BOM 找 LCSC 替代型号 → 重跑 component-selecting-JP
{%- endif %}
{%- elif user_intent.channel == "jp_domestic_fast" %}
- 你偏好日本仓，LCSC 覆盖率不影响主流程；PCB 单独 JLCPCB 打即可
{%- else %}
- {% if "lcsc" in coverage.recommended_paths %}LCSC 全覆盖，自动选 JLCPCB turnkey{% else %}LCSC 不全，自动选 PCBWay 或 PCB 独打{% endif %}
{%- endif %}

完整 fab vendor 规则：`.claude/skills/release/references/fab_internals.md` 或工作区 CLAUDE.md。
